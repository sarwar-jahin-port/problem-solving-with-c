// ctrl + shift + N -> keyboard shortcut(KS) to open new folder.
// ctrl + s -> KS to save file
// to check which compiler are you using settings > compiler(converts our code to computer understandable format.)

#include <stdio.h>
int main()
{
    printf("NAME: MOHAMMAD SARWAR JAHIN\nBATCH: CSE-54\nSECTION: D"); // here whatever we write into the double quotation("") will display as output. Ans (\n) means go to next line.

    return 0;
}
// line 5-9 please remember it. We will understand its meaning(except line 8) end of this semester(sem). And at line 6 int we will understand it at 6th sem.
